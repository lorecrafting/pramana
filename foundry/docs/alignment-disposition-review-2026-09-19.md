# Independent review of the frozen Foundry alignment disposition

**Verdict: PASS.** The exact staged 12-path documentation candidate identified below
can be committed as the authoritative controlled rewrite without a blocking correction.
This verdict accepts the documentation disposition only. It does not accept an FR-08
implementation, certify current process cleanup, authorize provider/host actions, or
establish deployment or activation readiness.

Reviewed 2026-09-19 by the independent `/root/foundry_plan_review` session. The reviewer
did not edit candidate files, the index, refs or worktrees. The only authored artifact
is this report. Unrelated untracked `apps/` remained untouched.

## Exact candidate identity

| Item | Independently verified identity |
|---|---|
| Base HEAD | `2f603675e3feb1a65f0ce57a3bd69aa93deec29d` |
| Base tree | `73bc4bd386cf67d61cf8563814a45d1c1c9e0af0` |
| Candidate record | `foundry/docs/alignment-disposition-2026-09-19.md` |
| Candidate-record SHA-256 | `cc66b07aa1ad73a000acb1d354ac58ea85bb3444a296a460766489dee6d3ad35` |
| Complete staged binary diff SHA-256 | `bbd15253368d8d44513d8ebb5d6172e7814762b0e29d34ac3f4299c85a43211a` |
| Eleven-path substantive inventory SHA-256 | `026900912764211a5735fda51bd7a7d8230d73d068f0d774d0855741ef96d19c` |
| Eleven-path substantive binary diff SHA-256 | `53727b23723121c03c230df72cc7c0c875cb426cd5b0843a83e3f37a2f5d140c` |
| Preserved alignment audit SHA-256 | `c825b22bb857ccccd08171d79fae3b2d33ce76025fdf7dcb5db91ecc3ff63fe7` |

Every inventory entry matches its staged blob. Working files match the index: no
unstaged tracked changes exist. The audit is byte-identical to
`/tmp/foundry-alignment-audit-2f60367.md`. Complete staged statistics are 12 files,
1074 insertions and 89 deletions; excluding the 80-line candidate record reproduces
11 files, 994 insertions and 89 deletions. The audit contributes 596 insertions.
Local HEAD/main/origin/main/origin/HEAD all identify the base commit. This checks cached
refs and the supplied pushed-base provenance; no fresh remote attestation was performed.

## Adversarial disposition

1. **Single backlog and original obligations:** REPAIR-PLAN explicitly remains the sole
   ticket/status/dependency authority. The audit is a preserved proposal/evidence record;
   H0/F are checkpoints and A/B labels are slices of existing parents. There remain 23
   parent ticket identities, including FR-15a, with no invented FR-23/24. I compared the
   old and staged routing tables: all 24 finding rows retain exactly the same owners
   AND evidence cells. All 43 original Acceptance, V2 acceptance-refinement and R4a
   acceptance-trace paragraphs remain verbatim. Diff inspection found no removed
   outcome, scope or exclusion obligation; the new scope and parent-inheritance text
   expands or allocates work rather than erasing it.

2. **Handoff contradiction:** H0 requires an evidence-complete report of the accepted
   v9 public boundary, not seven supported capabilities. Its report can honestly say
   unavailable/blocked while the reporting checkpoint completes. FR-08A owns schema/API
   completion and must pass meaningful positive and hostile full-gate probes at the new
   reviewed revision before FR-08B begins. H0 provenance is retained, so later source
   cannot masquerade as accepted v9. Direct SQL, empty-table inference and synthetic
   passing providers are expressly forbidden. An always-refusing implementation cannot
   pass FR-08A. This resolves the former impossible prerequisite without a circular gate.

3. **FR-06 contract and authority:** Removing only the added current-status block
   reproduces the original WORKFLOW-CONTRACT bytes and SHA-256
   `5d1621ff98fcb1aad2d6d37ba523b2a00c6bf90120873ffeb2518f23fd831488`.
   R1-R5 remain binding: issued/unknown effects, authenticated harness/tool separation,
   autonomously updatable domain kernel under a fixed root, sealed result/exit precedence,
   role-specific non-start recovery and conserved generations. FR-08A/B explicitly own
   protected controls, read sets, inbox, receipts and ledgers without introducing a second
   store or reducer. FR-13/14 retain full-spec/candidate custody, actual ancestry/full
   diff, mandatory receipts, independent review and protected ref CAS. FR-17/20/22 still
   require actual autonomous kernel repair under unchanged root policy and rollback
   preserving newer acknowledged work.

4. **Dependency sufficiency:** Independent parsing/traversal reproduced 29 table nodes,
   78 edges, zero unknown dependencies and no cycle. FR-22's transitive closure includes
   every other table node. F/FR-15aA allow bounded feasibility evidence; FR-15aB requires
   FR-08B and actual local isolation, while FR-09 then requires FR-15aB and FR-18A for
   installed-harness subscription conformance. Local fake endpoint tests can establish
   isolation before that real-route conformance, so the order does not require FR-09 to
   pass itself. Unsupported feasibility is not production completion. FR-18A and FR-19A
   precede FR-17; FR-18B precedes FR-19B; FR-17 explicitly precedes FR-20. The conditional
   FR-15-to-FR-13 edge removal is backed by explicit core interface ownership in
   FR-08A/B and FR-15aB, with an instruction to restore the edge before FR-13 if that
   ownership is not implemented. FR-17 and final closure still require full FR-15.

5. **Slice inheritance:** FR-15aA's inventory/specification cannot replace FR-15aB's
   actual principal/channel/auth/network denial and positive scoped use. FR-18A's honest
   observations cannot close the full producer/store/board/usage chain. FR-19A's backup
   and physical-failure baseline cannot close retention, compaction or cross-device
   relocation. Original parent paragraphs and explicit global inheritance retain all
   acceptance and exclusions, and FR-22 names both slices of each parent.

6. **ProcessGroup:** The current false-death defect is expressly open under FR-04/10.
   Historical FR-04 PASS remains bound to its original candidate; current source is not
   recertified by that PASS. Predicate/caller correction includes live argv marker,
   zombie, absent/recycled identities, unknown observation and Checks.Runner cancellation.
   It is required before reuse for new quiescence/retry guarantees; descendants and
   issuer/channel reconciliation remain FR-10. The reviewed CI foundation is not
   falsely revoked because a later process predicate needs correction.

7. **Provider and review policy:** Pi-first feasibility does not silently replace the
   governing OMP contract. Explicit reviewed substitution, actual subscription-route
   evidence, per-request accounting, credential separation and appropriate host authority
   remain prerequisites. No paid fallback or launch permission is added. Current review
   tiers match the supplied disposition: Sol-medium implementation, Sol-high routine
   independent review, Astra-high first critical review, Astra-medium narrow critical
   re-review and Astra-high re-escalation/FR-22. Historical review labels, including
   Sol-medium and Astra-medium reviews, remain historical rather than being relabelled.

8. **Status and provenance:** The candidate preserves original audit bytes and dated
   review/acceptance evidence. The revised navigation distinguishes accepted source,
   local incorporation, supplied/cached pushed-base identity and unknown loaded release.
   It does not convert historical test totals into freshly executed or deployed evidence.
   Full handoff readiness, real host/provider conformance, activation and FR-22 remain
   unproved. The candidate record excludes itself from its substantive digest explicitly;
   the complete staged-diff digest separately binds that record, avoiding a recursive hash.

## Nonblocking suggestions

- `docs/PLAN.md:325` compresses later work as activation followed by finishing steering.
  A future wording cleanup could say "complete steering and observation/storage baselines
  before activation; then finish switching, maintenance, improvement and closure." The
  controlling REPAIR-PLAN FR-17 row already requires FR-15, and the summary expressly
  defers to that graph, so this wording does not authorize premature activation and is
  not a commit blocker.
- Preserve the temporary v9 independent probe helpers as durable evidence when preparing
  the next handoff candidate, as the exact audit recommends. This documentation candidate
  does not claim to have archived or rerun them, and their absence from its changed paths
  does not erase the retained acceptance requirements.

Neither suggestion is a condition on this PASS. If the frozen candidate is edited to
address either, update its identity and validate the changed candidate appropriately.

## Checks and limits

- Read the repository/shared/Foundry instructions, full exact alignment audit, full staged
  repair plan, full workflow contract, full implementation log, FR-08 investigation and
  changed navigation/status material; inspected the complete relevant staged deltas.
- `elixir bin/check_docs.exs`: exit 0, **80 passed**, seed 704954, 9.2 seconds. This is
  documentation/layout evidence, not the Foundry runtime suite.
- `git diff --cached --check`, `git diff --exit-code`, and audit `cmp`: each exited 0.
- Independently parsed the dependency inventory and performed DFS cycle/unknown and
  FR-22 reachability checks; compared all F01-F24 routing cells and original acceptance
  paragraphs to `git show HEAD:foundry/docs/REPAIR-PLAN.md`.
- Reconstructed the inventory by taking the record's ordered hash/path rows, verifying
  each against `git show :PATH`, and hashing UTF-8 lines `HASH  PATH\n`. Recomputed the
  substantive binary diff with `git diff --cached --binary --` followed by those eleven
  paths. Recomputed the complete diff with `git diff --cached --binary`.
- Rechecked final status and complete staged-diff digest: unchanged from initial review.

No Foundry suite, daemon, listener/credential inspection, host provisioning, provider
request, Git promotion, activation or physical storage fault was run. This is an
independent documentation/contract/routing review of the exact candidate, not a new
runtime correctness or deployment certification.
