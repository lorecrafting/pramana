#!/bin/sh
# Explicit serving entrypoint. Migrate the intended database separately before boot;
# this command neither creates a database nor changes its schema.
set -eu
cd -P -- "$(dirname -- "$0")"
export PHX_SERVER=true
exec ./pramana start
