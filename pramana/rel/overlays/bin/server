#!/bin/sh
# Starts the release. Named `server` rather than `start` because a release's own `start`
# does not run migrations, and a public node that boots against an unmigrated database
# fails in a way that looks like a corpus problem.
set -eu
cd -P -- "$(dirname -- "$0")"
exec ./pramana start
