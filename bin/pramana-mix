#!/usr/bin/env bash
# Repository entry point for Pramāṇa only. Foundry has its own Mix project.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT/pramana"
exec mix "$@"
