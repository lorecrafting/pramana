#!/usr/bin/env bash
# Compatibility entry point. The implementation is owned by pramana/.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$ROOT/pramana/bin/pramana-modal" "$@"
